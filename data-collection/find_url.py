#this class is responsible for finding url from request_paramaters

def get_urls(request_paramaters):
    #use google api/scraping to find relavent urls?
    # if(request_source == reddit)
    # if(request_source == bugzilla)
    # if(request_source == all)
    return list(tuple(url, site))
